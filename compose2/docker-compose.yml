version: '3.3'
volumes:
  db_data: {}
services:
  db: # container do mysql
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    env_file:
      - ./db.env

  wordpress:
    depends_on:
      - db
    image: wordpress:latest
    ports:
      - 8000:80
    restart: always
    env_file:
      - ./wordpress.env